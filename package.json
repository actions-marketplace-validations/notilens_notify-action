{
  "name": "notilens-action",
  "version": "1.0.0",
  "description": "NotiLens GitHub Action",
  "private": true,
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist/bundle --minify && cp dist/bundle/index.js dist/index.js",
    "package": "npm run build"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}
